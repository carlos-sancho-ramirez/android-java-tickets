<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:name=".TicketsApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/appName"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light">

        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".activities.EditTicketActivity"/>
        <activity
            android:name=".activities.NewProjectActivity"
            android:label="@string/newProjectActivityTitle"/>

        <activity
            android:name=".activities.NewTicketActivity"
            android:label="@string/newTicketActivityTitle"/>

        <activity
            android:name=".activities.ProjectPickerActivity"
            android:label="@string/projectPickerActivityTitle"/>

        <activity android:name=".activities.TicketActivity"/>
    </application>
</manifest>